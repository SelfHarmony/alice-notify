# Секреты — НИКОГДА не коммитим
.env
.session.json
*.session
*.session-journal
userbot.session*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Логи проб
*.log
scripts/*.out

# Docker volume с кэшем сессии
data/

# Lock-файл uv — проект на pip, не коммитим (появляется, если запускали uv)
uv.lock

# Архив экспериментов с Telegram (не часть рабочего проекта)
scripts/legacy/
